# Design Changelog

This file records every change made to the domain-design package (`DESIGN.md`, `REQUIREMENTS.md`) for the Architecture Checkpoint. For each entry:

- **Artifact** names the file and section changed.
- **Deliverable** cites the corresponding Design Checkpoint deliverable (§5, 1–8).
- **Why** states the architecture or integration constraint that required the change.
- **Non-negotiable preserved?** confirms no Design Checkpoint invariant was weakened or dropped.

---

| # | Artifact changed | Deliverable | Why | Non-negotiable preserved? |
|---|---|---|---|---|
| 1 | `DESIGN.md` §3.1 — `GameResultPublished` integration event: replaced `isAbandoned: bool` with `eloEligible: bool`; `MatchResultPublished` integration event: added `outcome ∈ {completed, abandoned, forfeit_all}`. | Deliverable 4 — Commands and domain events catalog | Architecture requires the abandoned-vs-completed and tournament-vs-casual distinctions to be carried explicitly in the integration event contract so `ranking-service` needs no domain knowledge to filter. `eloEligible` is a superset of `isAbandoned`: it is `false` for abandoned casual games AND for all tournament games. | Yes. Elo scope (no tournament games, no abandoned casual games) is unchanged; the rule now travels in the contract rather than being re-derived at the consumer. |
| 2 | `DESIGN.md` §3.1 — Added `Match` as an explicit entity within the `Room` aggregate, distinct from `GameInstance`, with persisted state `{ matchId, roomId, players[], scoreline[playerId→wins], status, currentGameId, gamesPlayed }`. | Deliverable 3 — Aggregates and bounded contexts | Architecture co-locates the best-of-three series state machine with `GameInstance` inside the same actor mailbox and outbox transaction, guaranteeing log-before-broadcast atomicity across the series boundary (`ARCHITECTURE.md` §5.1.3, ADR-12). A cross-Kafka model was explicitly rejected because it would lose this atomicity. | Yes. Best-of-three semantics, top-3 advancement, tie-breakers, and Elo-per-game are unchanged. |
| 3 | `DESIGN.md` §3.1 internal events catalog: added `ChallengeWindowExpired` (dedup key `{gameId, challengeId}`) and `ReconnectionTimerExpired` (dedup key `{roomId, sessionId, openedAt}`) as first-class events. | Deliverable 4 — Commands and domain events catalog | Architecture requires durable, idempotent timer expiry with explicit dedup keys so a double-fire under pod failover cannot apply penalties twice (`ARCHITECTURE.md` §5.1.2, ADR-11). | Yes. The 5-s and 60-s windows are unchanged; idempotency strengthens the invariant rather than weakening it. |
| 4 | `DESIGN.md` §3.6 — `SessionInvalidated` event payload: added `prev_session_id` field. | Deliverable 4 — Commands and domain events catalog | Architecture's push-invalidation path requires the previous session id to perform an O(1) lookup in the `realtime-edge` connection table and close the correct SSE socket (`ARCHITECTURE.md` §5.6.1). Without the field the edge would need to scan all open connections. | Yes. Single-active-session is strengthened: the superseded stream is now explicitly closed rather than merely having its token invalidated in storage. |
| 5 | `DESIGN.md` §2.3.1 — Spectator boundary treatment: stated that privacy is enforced at **projection construction time**, not delivery time. | Deliverable 6 — Consistency strategy and edge cases | Architecture builds `SpectatorView` by filtering private events before writing to the projection store, so a delivery-side authorization bug cannot leak hand data (`ARCHITECTURE.md` §5.4). | Yes. The privacy guarantee (spectators never see hands) is preserved and architecturally hardened. |
| 6 | `DESIGN.md` §8.2 — Open question Q3 (tournament-placement rating formula): noted that the architecture isolates the formula to `ranking-service` internals with no cross-context contract dependency. | Deliverable 8 — Open questions and assumptions | Architecture decision isolates the formula change surface to one service, reducing cross-context contract risk when the formula is eventually defined. | N/A. Open question, not a non-negotiable. |
| 7 | `DESIGN.md` §1.1 (EventStorming "After GameCompleted"), §3.1 (invariants), §4.1 (Room Creation to Completion narrative steps 7–8), §7.10 (design decision), §8.1 assumption A7 — match series changed from **always-3-games** to **best-of-three with early termination at 2 wins**. `REQUIREMENTS.md` FR-T6 updated from "three-game match" to "best-of-three match (early termination at two wins)". | Deliverables 3, 5, 7, 8 — Aggregates; Domain flows; Design decisions; Open questions and assumptions | The architecture assignment explicitly requires "early termination at two wins" (assignment §2, match-series coordination invariant). The prior design decision to always play 3 games directly contradicts the assignment's stated invariant. `REQUIREMENTS.md` FR-R19 ("best-of-three series") implies early termination under standard terminology; the ambiguity with FR-T6 ("three-game match") is resolved in favour of FR-R19. | Yes. Top-3 advancement, tie-breaking (game wins → cumulative card-point total → earliest completion time), and Elo-per-game are all unchanged; the tie-breaking rules are game-count agnostic and work correctly whether a match ends after 2 or 3 games. |
| 8 | `DESIGN.md` §3.1 internal events catalog: added `MatchEnded` as a first-class internal event. Payload: `{ matchId, roomId, outcome ∈ {completed, abandoned}, winnerId, scoreline }`. `MatchCompleted` is retired; `MatchResultPublished` remains the cross-context integration event. | Deliverable 4 — Commands and domain events catalog | `ARCHITECTURE.md` §5.1.3 and §15.2 use `MatchEnded` as the internal signal emitted by the `Match` entity when the series concludes (either by early termination at 2 wins or after game 3). It is distinct from `MatchResultPublished` to make the internal state transition visible in the event log separately from the cross-context fan-out contract. | Yes. `MatchResultPublished` remains the authoritative cross-context contract consumed by tournament-service, ranking-service, spectator-service, and audit-service. `MatchEnded` is internal to Room Play only. |

---

## Package-level affirmation

No Design Checkpoint non-negotiable domain guarantee was weakened or dropped by any of the above changes. Specifically:

- **Elo scope** (no tournament games, no abandoned casual games): enforced more strongly via the `eloEligible` contract field (entry 1).
- **Tournament advancement** (top-3, best-of-three series, tie-break): preserved; early termination (entry 7) is consistent with standard best-of-three semantics and the assignment's stated invariant.
- **Single-active-session**: strengthened via the push-invalidation path (entries 4, 5).
- **Log-before-broadcast**: unchanged; the outbox transaction is the architectural home of this invariant.
- **Spectator privacy**: hardened from delivery-time to construction-time enforcement (entry 5).
- **Sequence-number enforcement**: unchanged.
- **60-s reconnection and 5-s Uno! windows**: unchanged in duration and semantics; idempotency of expiry strengthened (entry 3).
